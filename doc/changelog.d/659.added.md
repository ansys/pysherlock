Add simplified version increment checker that validates if developers have manually incremented the version before pushing changes. The checker provides warnings but allows pushes to continue, with multiple skip options available v3.