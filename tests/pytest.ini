
[pytest]
markers =
    behavioral: tests that require a local Sherlock install / real process launch
    requires_sherlock: tests that depend on Sherlock binaries or environment
filterwarnings =
    # Silence the known deprecation coming from ansys.sherlock.* to keep logs clean
    ignore:.*PartsListSearchMatchingMode.*is deprecated.*:DeprecationWarning
